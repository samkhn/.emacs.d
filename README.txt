Samiur's Emacs Config

Targets Emacs 28.

Assumes .emacs.d will get written to system ~ (so %APPDATA% or /home/username).

Rule: NO MELPA. Ships with packages such as rust, cmake, llvm, llvm::tablegen
